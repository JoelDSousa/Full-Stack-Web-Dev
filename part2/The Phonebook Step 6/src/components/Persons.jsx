const Persons = ({ person }) => {
  //console.log('NOTE');
  //console.log(note);

  if(person.show === true){
    
    
    return (
      
      <li>{person.name} {person.number}</li>
    )}
  }
  
  export default Persons